{
  "name": "vitals-ledger",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "deploy": "vite build && npx gh-pages -d dist --dotfiles"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.4.10",
    "vite-plugin-pwa": "^0.20.5"
  }
}
